# Snake_case for business identifiers

- All business identifiers (themes, usages, types, tailles) use snake_case
- Custom user input normalized with `\s+` → `_` and `.toLowerCase()`
- Never use kebab-case for domain identifiers
- Ref: DEC-001
